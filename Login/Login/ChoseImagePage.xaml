<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Login.ChoseImagePage">
    <ContentPage.Content>
        <StackLayout>
            <CollectionView x:Name="collectionView" ItemsSource="{Binding filePaths}">
                <!--<CollectionView.ItemTemplate>
                    <DataTemplate>
                        <StackLayout>
                            <Grid Padding="10" RowDefinitions="Auto, 20">
                                <Label Grid.Row="0" Grid.Column="0" Text="{Binding FilePathLol}" FontSize="Medium"/>
                                --><!--<Image Source="{Binding imageUrl}">
                                    --><!--<Image.GestureRecognizers>
                                        <TapGestureRecognizer Tapped="OnTapGestureRecognizerTapped" NumberOfTapsRequired="1" />
                                    </Image.GestureRecognizers>--><!--
                                </Image>--><!--
                            </Grid>
                        </StackLayout>
                    </DataTemplate>
                </CollectionView.ItemTemplate>-->
            </CollectionView>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>